import { useAuth } from "@/context/auth-context";

// Re-export for easier import paths if needed, strictly speaking we defined the hook in the context file 
// but sticking to the plan of having hooks/use-auth.ts is good practice.

export { useAuth };
